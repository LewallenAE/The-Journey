const monthlyRent = 500;

const yearlyRent = monthlyRent * 12;
console.log('If your monthly rent is: ' + monthlyRent + ', then your yearly rent is: ' + yearlyRent);



// Never use var anymore only let and const....